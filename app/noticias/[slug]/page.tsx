import Link from"next/link";import{Contact,Footer,Header,news}from"../../ui";export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params,n=news.find(x=>x.slug===slug);if(!n)return <main><Header/><section className="inner"><h1>Noticia no encontrada</h1></section><Footer/></main>;return <main><Header/><article className="post"><small>{n.category} · {n.date}</small><h1>{n.title}</h1><p className="lead">{n.excerpt}</p>{n.body.map(p=><p key={p}>{p}</p>)}<blockquote>Información oficial de COOPSAR.</blockquote><Link href="/noticias">← Volver a noticias</Link></article><Contact/><Footer/></main>}
