"use client";

import { useEffect, useState } from "react";
import { funeralRelationshipLabel, funeralRequestStatuses, funeralStatusLabel, type FuneralRequestStatus } from "../../../../lib/funeral-family-update";

type ListRow = { id: string; requestNumber: string; memberNumber: string; holderName: string; dni: string; phone: string; status: FuneralRequestStatus; createdAt: string };
type Detail = { request: { id: string; request_number: string; member_number: string; holder_full_name: string; holder_dni: string; phone: string; email: string | null; status: FuneralRequestStatus; consent: boolean; created_at: string }; members: Array<{ id: string; full_name: string; dni: string; birth_date: string; relationship: "spouse" | "cohabitant" | "child" | "parent" | "other" }>; audit: Array<{ id: string; action: string; old_status: string | null; new_status: string | null; actor_email: string | null; created_at: string }> };
type Metrics = { total: number; new: number; inReview: number };

export function FuneralFamilyInbox() {
  const [rows, setRows] = useState<ListRow[]>([]), [selected, setSelected] = useState<Detail | null>(null), [error, setError] = useState(""), [feedback, setFeedback] = useState(""), [loading, setLoading] = useState(true), [saving, setSaving] = useState(false), [search, setSearch] = useState(""), [statusFilter, setStatusFilter] = useState(""), [metrics, setMetrics] = useState<Metrics>({ total: 0, new: 0, inReview: 0 });
  async function load() { setLoading(true); const params = new URLSearchParams(); if (search) params.set("q", search); if (statusFilter) params.set("status", statusFilter); const response = await fetch(`/api/admin/sepelio/planillas?${params.toString()}`, { cache: "no-store" }); const data = await response.json().catch(() => ({})); setLoading(false); if (!response.ok) { setError(data.error || "No pudimos cargar las solicitudes."); return; } setError(""); setRows(data.requests || []); setMetrics(data.metrics || { total: 0, new: 0, inReview: 0 }); }
  useEffect(() => { let active = true; const params = new URLSearchParams(); if (statusFilter) params.set("status", statusFilter); void fetch(`/api/admin/sepelio/planillas?${params.toString()}`, { cache: "no-store" }).then(async (response) => ({ response, data: await response.json().catch(() => ({})) })).then(({ response, data }) => { if (!active) return; setLoading(false); if (!response.ok) { setError(data.error || "No pudimos cargar las solicitudes."); return; } setError(""); setRows(data.requests || []); setMetrics(data.metrics || { total: 0, new: 0, inReview: 0 }); }).catch(() => { if (active) { setLoading(false); setError("No pudimos cargar las solicitudes."); } }); return () => { active = false; }; }, [statusFilter]);
  async function open(id: string) { const response = await fetch(`/api/admin/sepelio/planillas?id=${encodeURIComponent(id)}`, { cache: "no-store" }); const data = await response.json().catch(() => ({})); if (!response.ok) { setError(data.error || "No pudimos cargar el detalle."); return; } setSelected(data); }
  async function update(status: FuneralRequestStatus) { if (!selected) return; setSaving(true); setFeedback(""); const response = await fetch("/api/admin/sepelio/planillas", { method: "PATCH", headers: { "content-type": "application/json" }, body: JSON.stringify({ id: selected.request.id, status }) }); const data = await response.json().catch(() => ({})); setSaving(false); if (!response.ok) { setError(data.error || "No pudimos actualizar la solicitud."); return; } setError(""); await Promise.all([open(selected.request.id), load()]); setFeedback(data.unchanged ? "El estado ya estaba actualizado." : "Estado actualizado."); window.setTimeout(() => setFeedback(""), 4000); }
  return <section className="admin-content funeral-admin"><p className="eyebrow">Sepelio</p><h1>Actualizaciones de grupo familiar</h1><p className="admin-intro">Solicitudes privadas. Los datos completos sólo se muestran al abrir un caso.</p>{error && <p className="admin-error" role="alert">{error}</p>}{feedback && <p className="admin-success" role="status">{feedback}</p>}<div className="admin-metrics" aria-label="Resumen de solicitudes"><span><strong>{metrics.new}</strong>Nuevas</span><span><strong>{metrics.inReview}</strong>En revisión</span><span><strong>{metrics.total}</strong>Total</span></div><div className="funeral-admin-layout"><div className="funeral-admin-list"><div className="admin-list-heading"><h2>Solicitudes recientes</h2><form onSubmit={(event) => { event.preventDefault(); void load(); }}><label>Buscar<input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Solicitud, titular o referencia" /></label><label>Estado<select value={statusFilter} onChange={(event) => { setLoading(true); setStatusFilter(event.target.value); }}><option value="">Todos</option>{funeralRequestStatuses.map((status) => <option key={status} value={status}>{funeralStatusLabel(status)}</option>)}</select></label><button type="submit">Buscar</button></form></div>{loading ? <p>Cargando solicitudes…</p> : rows.length === 0 ? <p>No hay solicitudes registradas.</p> : <ul>{rows.map((row) => <li key={row.id}><button type="button" onClick={() => void open(row.id)}><span><strong>{row.requestNumber}</strong><small>{row.holderName} · DNI {row.dni} · Tel. {row.phone} · {new Date(row.createdAt).toLocaleDateString("es-AR")}</small></span><em>{funeralStatusLabel(row.status)}</em></button></li>)}</ul>}</div><aside className="funeral-admin-detail" aria-live="polite">{!selected ? <p>Seleccioná una solicitud para ver el detalle.</p> : <><p className="eyebrow">{selected.request.request_number}</p><h2>{selected.request.holder_full_name}</h2><dl><div><dt>DNI</dt><dd>{selected.request.holder_dni}</dd></div><div><dt>Teléfono</dt><dd>{selected.request.phone}</dd></div><div><dt>Correo</dt><dd>{selected.request.email || "No informado"}</dd></div><div><dt>Referencia</dt><dd>{selected.request.member_number}</dd></div></dl><h3>Integrantes</h3><ul>{selected.members.map((member) => <li key={member.id}>{member.full_name} · {funeralRelationshipLabel(member.relationship)} · DNI {member.dni}</li>)}</ul><label>Estado<select value={selected.request.status} disabled={saving} onChange={(event) => void update(event.target.value as FuneralRequestStatus)}>{funeralRequestStatuses.map((status) => <option key={status} value={status}>{funeralStatusLabel(status)}</option>)}</select></label><h3>Auditoría</h3><ul>{selected.audit.map((item) => <li key={item.id}>{item.action === "status_changed" ? `Estado: ${funeralStatusLabel(item.old_status as FuneralRequestStatus)} → ${funeralStatusLabel(item.new_status as FuneralRequestStatus)}` : "Solicitud creada"} · {new Date(item.created_at).toLocaleString("es-AR")} · {item.action === "status_changed" ? "por administrador" : ""}</li>)}</ul></>}</aside></div></section>;
}
