# COOPIA knowledge and safety

COOPIA uses official COOPSAR content and server tools. It must not invent prices, coverage, outages or requirements. Service status is selected from currently active published alerts. The default response allowance is two per session and is configurable with `AI_SESSION_LIMIT`. No full conversations or sensitive fields are written to journey analytics.
